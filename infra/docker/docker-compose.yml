version: "3.9"

services:
  lavalink:
    image: ghcr.io/lavalink-devs/lavalink:4
    container_name: lavalink
    restart: unless-stopped
    environment:
      SERVER_PORT: ${LAVALINK_PORT:-2333}
      LAVALINK_SERVER_PASSWORD: ${LAVALINK_PASSWORD:-youshallnotpass}
    ports:
      - "${LAVALINK_PORT:-2333}:${LAVALINK_PORT:-2333}"
    volumes:
      - ./lavalink/application.yml:/opt/Lavalink/application.yml:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS -H 'Authorization: $${LAVALINK_SERVER_PASSWORD}' http://localhost:$${SERVER_PORT}/version >/dev/null"]
      interval: 30s
      timeout: 5s
      retries: 3
    # youtube-source is pinned in lavalink/application.yml and tested in CI.
  bot:
    build:
      context: ../..
      dockerfile: infra/docker/Dockerfile
    command: python -m elbot.main
    env_file: ../../.env
    environment:
      - LAVALINK_HOST=lavalink
      - LAVALINK_PORT=${LAVALINK_PORT:-2333}
      - LAVALINK_PASSWORD=${LAVALINK_PASSWORD:-youshallnotpass}
    depends_on:
      lavalink:
        condition: service_healthy

